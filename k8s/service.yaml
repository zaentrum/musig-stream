apiVersion: v1
kind: Service
metadata:
  name: musig-stream
  labels:
    app: musig-stream
spec:
  type: ClusterIP
  selector:
    app: musig-stream
  ports:
    - name: http
      port: 8080
      targetPort: http
